<pre>
<%perl>
    my $rs = $c->model('Harvest::Haruser')->search({ -or => [ realname=>{ -like=>'%RO%'}, username=>{ -like=>'%RO%' } ] });
    while( my $r = $rs->next ) {
        print $r->username . "," . $r->realname;
        print "\n";
    }

</%perl>

