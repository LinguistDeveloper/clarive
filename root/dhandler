<%args>
</%args>
<%perl>
</%perl>
% unless( $m->dhandler_arg ) {  ## /
%#<& /site/main.mas &>
%  my $req = $m->make_subrequest( comp => '/site/main.mas');
%   $req->exec;	
% } else {
%  eval { $c->res->status(404) };
 
% print eval { _loc('Page not found: %1', $c->req->path ) }; # may fail due to missing $c

% }
<%filter>
	#s/\n|\r//g
</%filter>
